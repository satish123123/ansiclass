---
-

 hosts: all
 # gather_facts: true # this will make not to gather facts before running tasks

 vars_files:
    service.yml
  
 tasks:  
  name: hello 
  win_service: >
        {% if item == "BITS" -%}
            name: item
            state: stopped
        {% elif item == "Dnscache" -%}
            name: item
            state: stopped
        {% else -%}
             This is not found
        {% endif %}
  loop:
          "{{serviceneedstostop}}"
             

...