---
-

 hosts: all
 # gather_facts: true # this will make not to gather facts before running tasks

 vars_files:
    service.yml
  
 tasks: 
  #  - name: bits service
    #  win_service: 
    - name: "{{item}}"
      debug:
       msg: >
              {% for services in "{{item}}" -%}
                  {{services}}
             {% endfor -%}
      loop:
          "{{serviceneedstostop}}"
             

...